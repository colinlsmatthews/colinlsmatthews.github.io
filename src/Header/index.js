import React from 'react';
import Nav from '../Nav';
import './index.css';

function Header() {
    return (
        <header>
            <h1 class="sans">Colin L.S. Matthews</h1>
            <Nav />
        </header>
    );
}

export default Header;
